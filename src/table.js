class Table {
  constructor(id, number, ispriority, full, listPeople) {
    this.id = id;
    this.number = number;
    this.ispriority = ispriority;
    this.full = full;
    this.listPeople = listPeople;
  }
}

export default Table;
